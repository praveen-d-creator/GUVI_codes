#include<stdio.h>
int main(){
  int a=1,b=1,n;

  printf("Enter a number: ");
  scanf("%d",&n);

  while(a<=num){
      b=b*a;
      a++;
  }

  printf("Factorial of %d is: %d",n,b);
  return 0;
}